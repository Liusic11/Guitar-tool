# 2026-08-15 工作日志

## 完成：乐句库 Lick Library（第 6 视图）+ CAGED G/C 形补全

用户拍板做 A（乐句库）+ D（CAGED G/C 形补全）。两项都落地并提交（commit f4acd02）。

### D · CAGED G/C 形补全（chords.ts）
- **背景**：`listChordVoicings` 原本只有 开放+E形(6弦)+A形(5弦)+D形(4弦) 4 候选。E/A/D 都是「根音在窗口低端」的横按形，而 G 形（开放 G：3-2-0-0-0-3）与 C 形（开放 C：x-3-2-0-1-0）的特点是**部分音落在根音品位之下**（比根音低 1~3 品），旧 `[base, base+3]` 窗口算法永远生成不出来。
- **实现**：`CAGED_SHAPES` 偏移模板 + `buildCagedVoicing` 就近取和弦音（每根弦按模板偏移算理想品位，±2 品窗口内取最近的**真实和弦音**）。
  - G 形（根音 6 弦，如 A 大 = 5-4-2-2-2-5）：三和弦 [0,-1,-3,-3,-3,0]；七和弦 1 弦改 7 音 [0,-1,-3,-3,-3,-2]（G7 = 3-2-0-0-0-1）
  - C 形（根音 5 弦，6 弦闷，如 D 大 = x-5-4-2-3-2）：三和弦 [null,0,-1,-3,-2,-3]；七和弦 3 弦改 7 音 [null,0,-1,0,-2,-3]（C7 = x-3-2-3-1-0）
- **强度校验**：七和弦必须含 7 音；♭5/♯5 类色彩和弦（m7b5/dim7/dim/aug）必须含色彩音，找不到就整体不提供该形状（确定性、不硬凑）。
- `ChordPosition` 加 `'g' | 'c'`；`ChordChanges` POSITION_OPTIONS 加「G 形（6弦伸展）/ C 形（5弦伸展）」锁把位选项。
- **验证**：esbuild 打包跑脚本，168 个 G/C 把位（9 类型 × 12 根音 × 2 形状）**0 失败**——每个音都是该和弦的真实和弦音、根音必在。

### A · 乐句库 Lick Library（第 6 视图）
- **定位**：用户「词汇 → 造句」框架里缺的中间层——别人写好的句子。工具此前只教词汇（和弦/音阶/把位）+ Jam 自由即兴，没有「手写乐句」。
- **数据层 `src/lib/licks.ts`**：20 条手写乐句（蓝调/funk/爵士/摇滚各 5），全以 A（rootPc 9，6 弦 5 品）为参考根音书写绝对品位，`transposeLick` 按锚定弦（6 弦）根音品位差整体平移，12 根音全部可移（移调后度数不变）。含 `why`（老师口吻，讲用了哪几个音、为什么好听）+ `tip` + `worksOver`（适用和弦类型）+ `timing`。
  - 蓝调 5 句：开门句（滑音）/ BB 哭腔（3弦7品推全音）/ 蓝调音句（5弦6品♭5）/ 五声下行 / 三连音句
  - Funk 5 句：Funk 咬合（反拍重音）/ 属七 Funk（根音-♭7 骨架）/ 幽灵音句 / 切分句 / 单弦 16 分
  - 爵士 5 句：多利亚爬升 / 半音趋近（G#-A）/ 琶音句（Am7）/ 摇摆句 / 导音落点
  - 摇滚 5 句：开门 Riff / 强力和弦跳 / 推弦句 / 五声跑动 / 结尾句
- **组件 `LickLibrary.tsx`**：顶栏第 6 视图「乐句」。
  - 顶部：风格筛选（全部/蓝调/Funk/爵士/摇滚）+ 根音 12 键（整句移调）
  - 左大栏：指板（compact，根音 answer/其余 secondary，当前播放 hit 高亮 + 弦振动）+ 播放/停止 + 逐音清单（弦·品·音名·级数 chips，播放时逐颗点亮）+ 「去 Jam 页练这个」
  - 右小栏：乐句列表（点选卡片）+ 老师拆解（why / 怎么练 / 适用和弦 / 节奏 / 参考根音）
  - 挂载时消费 `sessionStore.lickId`（Jam 跳转预选）
- **Jam 集成**：Jam 页右栏新增「🎵 现在这个和弦上能弹的乐句」——`licksForChordType(当前和弦类型, 3)`，点击 `setRoot + setLick + requestNav('licks')` 直达乐句页并预选该句。
- **session.ts**：新增 `lickId` 共享字段 + `setLick()` + 持久化；`ViewKey` 加 `'licks'`。
- **验证**：tsc --noEmit 通过；vite build 通过（61 模块，dist 已更新）；逻辑层脚本验证 20 乐句 × 12 根音移调全有效、度数保持、风格筛选 5×4、dom7/m7 推荐正确。

## 环境坑记录
- **vite preview/build 在沙箱下 `spawn EPERM`**：vite 加载配置时要 spawn esbuild 子进程（管道 stdio），沙箱拦截。`npm run build` 需 `danger-full-access` 权限才能过（已获批一次）；preview 服务器同理。**规避法**：写极简 Node 静态服务器（`serve-dist.cjs`，纯 http+fs 不 spawn）托管 dist/ 即可本地预览；但 puppeteer 启动系统 Chrome 同样 spawn EPERM，浏览器级冒烟在本沙箱做不了，改用 esbuild 打包 + node 跑逻辑层验证（数据正确性可比浏览器冒烟更严格）。
- 临时脚本用完即删（probe-*.mjs/cjs、serve-dist.cjs）。

## 待办（更新）
- 乐句库目前 20 条（5/风格），可随时扩充（licks.ts 数组追加即可）。
- 和弦识记训练（用户已会开放和弦，优先级低）。
- 练习路线/打卡页（更后期，仍是收口层最后一块）。
