# 2026-08-07 工作日志

## 吉他指板训练器 — 三题型扩展完成
- 项目路径：`D:/Desktop/Project/guitar_proejct`（Vite + React + TS，纯前端，Web Audio 实时合成拨弦音，无音频文件）
- 本次交付：在原有「找位置(find)」基础上新增 **认音名(name)** + **找八度(octave)** 两种模式，并接入 **SRS 加权抽题**
- 关键文件
  - `src/lib/music.ts`：题型 TaskType(find/name/octave)、Question.targets（含 srsKey 锚点）、generateQuestion 加权抽题
  - `src/lib/srs.ts`：新增记忆模型（scoreItem/updateMastery/pickWeighted/未见过优先）
  - `src/hooks/useQuizEngine.ts`：三题型交互、mastery 持久化 localStorage、统计按题型判定
  - `src/components/Fretboard.tsx`：新增 `reference` 高亮类型（参考格空心环、不泄音名）
  - `src/components/PromptStage.tsx`：音名键盘 + 八度标记进度 + 确认按钮
  - `src/App.tsx`：顶栏 task 分段器、handleFretClick 按题型分支、快捷键 f/n/o
  - `src/components/SettingsDrawer.tsx`：智能抽题开关 + 记忆强度 mini 热力图 + 重置记忆库
- 验证：`npm run build` 零错误（192KB/gzip 62KB）；dev server `http://127.0.0.1:5173/` 返回 200；已用 puppeteer-core + Chrome 无头截图验证 5 个界面（find/name/name-answered/octave/settings）视觉正确
- 后续未做：和弦模块（和弦构成音/辨识/换和弦计时，引擎已预埋 `strum()` 接口）
