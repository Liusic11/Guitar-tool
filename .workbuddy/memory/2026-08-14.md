# 2026-08-14 工作日志

## 完成：耳朵训练新增「鼓点 / 进行」两模式 + 全局父调贯通（Task #3）

- `src/lib/session.ts`：新增 `keyPc` / `keyQuality` / `jamPresetId` 共享字段 + `setKey()` / `setJamPreset()` + `useSharedKey()`；持久化一并写入 localStorage。
- `src/components/JamTrainer.tsx`：进入即 `setKey(preset.keyPc, preset.keyQuality)`；挂载时优先消费 `session.jamPresetId`（耳朵训练「去 Jam 页练这个」落点）。
- `src/components/EarTrainer.tsx`：新增 `groove`（听鼓点→猜律动，复用 `audioEngine.playGroove`，选项为 4 个鼓点 label）与 `progression`（听和弦进行→猜父调，复用 `voiceChord`+`strum(when)` 扫两遍，选项为 4 个候选父调）；揭示后把父调+父音阶写进共享，并提供「去 Jam 页练这个」按钮。
- 修复隐藏多年（从未跑过 build）的潜在类型错误：`Fretboard` HighlightKind 缺 `accent`；`audio.ts` `pluck` 中 `source` 在声明前使用（detune 引入）；`JamTrainer`/`progressions` 未用 import；`EarTrainer` scale 分支无 `if` 守卫导致 groove/progression 不可达（被 smoke test 抓出）。

## 验证
- `tsc --noEmit` 通过；`vite build` 通过（dist 已更新）。
- 用 puppeteer-core + 系统 Chrome 跑无头冒烟：耳训练 5 模式均渲染、groove=4 鼓点选项 / progression=4 父调选项、回答后揭示正常、「去 Jam 页练这个」能跳到 Jam 并加载对应进行（如 ii–V–I jazz）。无 pageerror（仅 favicon 404，无害）。

## 待办（用户原始 5 点已落地）
1. 和弦成团 ✓ 2. 命名 groove 库 ✓ 3. 音阶扩 6 个 ✓ 4. 鼓点多风格 ✓ 5. 模块交融（全局父调 + 耳朵→Jam）✓
下一步可选项：和弦识记训练（用户已会开放和弦，优先级低）；Jam 伴侣 / 练习路线打卡页（更后期）。

## 完成：右侧「老师口吻」内容大幅充实（针对用户乐理基础偏弱）
- **新增 `src/components/ConceptCheatSheet.tsx`**：贯穿三模块的术语表（概念小抄）。4 组共 16 条：基础(音级/音程/三度叠置/根音三音五音)、和弦(顺阶和弦/属七解决/II–V–I/可移动横按)、音阶(关系大小调/五声/蓝调音/调式/把位)、节奏(groove/反拍/swing)。`filter` 按模块显示相关子集。
- **接入**：`ChordTheory`(filter basic+chord)、`ScaleTrainer` 右栏(before 演示按钮, filter basic+scale)、`EarTrainer` 右栏(filter 全 4 组)；配套 CSS 加到 `src/index.css`（`.concept-sheet*`），复古制琴工坊纸感+黄铜/炭火红点缀，对齐 `.chord-theory__*` 字号。
- **扩充数据文案（更详细、术语当场解释、多给曲例）**：`scales.ts` 16 条音阶 theory/color/usage；`chords.ts` 9 个和弦类型全部字段；`harmony.ts` 的 CHORD_SCALE_MAP reason / PENTATONIC_CONTEXT why / PROGRESSIONS why。
- **补全 `ScaleTrainer` 内联 teacherTip**：原只覆盖 7 个音阶，新增 phrygian/lydian/locrian/harmonicMinor/melodicMinor/diminished/wholeTone/bebopDominant/bebopMajor 共 9 条（why/ear/songs/box），其余音阶不再回退通用文案。
- **扩充 `EarTrainer` 老师提示**：5 条练法各加「小任务」与术语解释（音程/和弦/音阶/鼓点/进行）。

## 验证
- `tsc --noEmit` 通过；`vite build` 通过。
- 无头冒烟：和弦页小抄 8 术语、音阶页 9 术语(且 teacherHead 正常非回退)、耳朵页 16 术语+5 条练法；三页均无 pageerror（仅 favicon 404）。

